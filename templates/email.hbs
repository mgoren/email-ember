<div class="email-display">
  <h1>{{title}}</h1>
  <h5>from: {{from}}</h5>
  <h5>to: {{to}}</h5>
  <p>{{body}}</p>
</div>

<p>{{#link-to 'assign-to-folder' classNames="btn btn-default"}}Assign to Folder{{/link-to}}</p>
{{outlet}}